% System Architecture: Simplex-Track perception pipeline
% CARLA Sensors → YOLOv9 → DeepSORT → Social Force EKF → TTC Monitor → Continue/E-Stop
\begin{tikzpicture}[
    node distance=0.6cm and 0.45cm,
    block/.style={rectangle, draw, fill=lightcyan!40, minimum height=0.7cm, minimum width=1.6cm, align=center, font=\scriptsize\sffamily, rounded corners=2pt, line width=0.5pt},
    sensor/.style={block, fill=goldcol!30},
    advanced/.style={block, fill=tealaccent!20},
    monitor/.style={block, fill=mauvecol!20},
    baseline/.style={block, fill=dangerzone!15},
    output/.style={block, fill=safezone!20},
    arrow/.style={-{Stealth[length=4pt]}, thick, color=darkteal},
    dasharrow/.style={-{Stealth[length=4pt]}, thick, dashed, color=dangerzone},
    label/.style={font=\tiny\sffamily\itshape, color=darkteal},
    grouplabel/.style={font=\scriptsize\sffamily\bfseries, color=darkteal},
]

% Row 1: Perception Pipeline (Advanced Channel)
\node[sensor] (carla) {CARLA\\Simulator};
\node[sensor, right=of carla] (rgb) {RGB\\Sensors};
\node[advanced, right=of rgb] (yolo) {YOLOv9\\Detector};
\node[advanced, right=of yolo] (deepsort) {DeepSORT\\Tracker};
\node[advanced, right=of deepsort] (sfekf) {Social Force\\EKF};

% Row 2: Decision Module
\node[monitor, below=0.9cm of sfekf] (ttc) {TTC\\Monitor};
\node[monitor, left=of ttc] (odd) {ODD\\Monitor};
\node[monitor, left=of odd] (uq) {Uncertainty\\Quantifier};

% Row 3: Output Controllers
\node[output, below=0.9cm of odd] (cont) {Continue\\Navigation};
\node[baseline, right=of cont] (estop) {Emergency\\Braking};

% Arrows: perception pipeline
\draw[arrow] (carla) -- (rgb);
\draw[arrow] (rgb) -- (yolo);
\draw[arrow] (yolo) -- (deepsort);
\draw[arrow] (deepsort) -- (sfekf);

% Arrows: to decision module
\draw[arrow] (sfekf) -- (ttc);
\draw[arrow] (sfekf) |- (uq);
\draw[arrow] (deepsort) |- (odd);

% Arrows: decision logic
\draw[arrow] (uq) -- (odd);
\draw[arrow] (odd) -- (ttc);

% Arrows: outputs
\draw[arrow, color=safezone] (ttc) -| node[label, near start, right] {$\rho(\varphi) \geq 0$} (cont);
\draw[dasharrow] (ttc) -| node[label, near start, left] {$\rho(\varphi) < 0$} (estop);

% Group boxes
\begin{scope}[on background layer]
    \node[draw=tealaccent, dashed, fill=tealaccent!5, rounded corners=4pt,
          fit=(yolo)(deepsort)(sfekf), inner sep=4pt,
          label={[grouplabel, anchor=south]above:Advanced Channel (Perception)}] {};
    \node[draw=mauvecol, dashed, fill=mauvecol!5, rounded corners=4pt,
          fit=(uq)(odd)(ttc), inner sep=4pt,
          label={[grouplabel, anchor=south]above:Decision Module}] {};
    \node[draw=safezone, dashed, fill=safezone!5, rounded corners=4pt,
          fit=(cont)(estop), inner sep=4pt,
          label={[grouplabel, anchor=south]above:Actuation}] {};
\end{scope}

\end{tikzpicture}
